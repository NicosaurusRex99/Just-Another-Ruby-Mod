modLoader="javafml" #mandatory
loaderVersion="[27,)" #mandatory (24 is 1.13-pre, 25 is 1.13.1-re, 26 is 1.13.2, 27 is 1.14.x)
issueTrackerURL="https://github.com/NicosaurusRex99/JARMGit/issues" #optional

[[mods]] #mandatory
	modId="ruby" #mandatory
	version="${version}" #mandatory
	displayName="Just Another Ruby Mod" #mandatory
	updateJSONURL="https://raw.githubusercontent.com/NicosaurusRex99/JARMGit/1.15.2/jarm_update.json" #optional
	displayURL="https://www.curseforge.com/minecraft/mc-mods/jarm-just-another-ruby-mod/files" #optional
	logoFile="ruby.png" #optional
	credits="Silthulu" #optional
	authors="NicosaurusRex99" #optional
	description='''I think the mod name says enough
'''

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.examplemod]] #optional
    # the modid of the dependency
    modId="forge" #mandatory
    # Does this dependency have to exist - if not, ordering below must be specified
    mandatory=true #mandatory
    # The version range of the dependency
    versionRange="[27,)" #mandatory
    # An ordering relationship for the dependency - BEFORE or AFTER required if the relationship is not mandatory
    ordering="NONE"
    # Side this dependency is applied on - BOTH, CLIENT or SERVER
    side="BOTH"
# Here's another dependency
[[dependencies.examplemod]]
    modId="minecraft"
    mandatory=true
    versionRange="[1.14.4]"
    ordering="NONE"
    side="BOTH"